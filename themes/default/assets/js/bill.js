$(document).ready(function () {

		// $('body').on('keyup focusout', '#billFormBody #billAmount', function () {

			
		// 	var modal = $(this).closest('#billFormBody');
			
		// 	//var allBillAmounts = modal.querySelectorAll('#billAmount')
		// 	var allBillAmounts = modal.closest('#billsModal').find('#billFormBody #billAmount');
		// 	var totalAmount =0;

		// 	allBillAmounts.each(
		// 		function(){
		// 			var value = Number($(this).val().replace(',', ''));
		// 			if (value) {

		// 				totalAmount += value;
		// 				// console.log('totalDebit is ' + totalDebit + 'value is ' + value);
		// 			}
		// 			var total = $(modal).closest('#billsModal').find('#billTotalAmount');
		// 			console.log(total)
		// 			total.html(totalAmount.toFixed(2).replace(/\d(?=(\d{3})+\.)/g, '$&,') ) ;
		
		// 		})
		
		// });

		// $('body').on('click', '#journalEntryBody tr', function () {

        //     fillEmptyRow($(this), ["#property_id","#unit_id","#description","#profile_id","#class_id"]);

        //  });

});